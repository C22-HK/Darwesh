{
  "name": "darwesh-tooling",
  "private": true,
  "type": "module",
  "description": "Test/migration tooling only. Nothing here runs at request time -- GitHub Pages serves the committed HTML/CSS/JS directly. `test:rules` runs the Firestore security-rules suite against the local emulator; firebase-admin is used only by scripts/backfill-public-coords.mjs, which a human runs by hand.",
  "scripts": {
    "test:rules": "firebase emulators:exec --only firestore --project demo-darwesh \"node --test --test-concurrency=1 tests/firestore/*.test.mjs\"",
    "check": "node scripts/ci-checks.cjs"
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^5.0.2",
    "firebase": "^12.18.0",
    "firebase-admin": "^14.3.0",
    "firebase-tools": "^15.28.2"
  }
}
